export default function () { //default şeklinde tanımlarsak isim vermemize gerek yok, 
// böylelikle index.js de bu fonk çağırırken istediğimiz ismi yazabiliyoruz, mesela orda Header ismini verdik
  const a = 5;
  const b = 10;

  const toplam = a + b;

  console.log(toplam);
}